[project]
name = "odoo-seeder"
version = "0.1.0"
description = "Realistic inventory and stock movement seeder for Odoo"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.31.0",
    "python-dotenv>=1.0.0",
]
